// Client-facing barrel over the shared roster module.
// Source of truth: supabase/functions/_shared/roster.ts (Deno-safe,
// imported by Edge Functions). Do not duplicate the retired list here.
export * from '@shared/roster.ts';
